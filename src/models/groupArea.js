export default class GroupArea {
    constructor(name, areas, color, group_area_id) {
        this.name = name;
        this.areas = areas;
        this.group_area_id = group_area_id;
        this.color = color;
    }
}